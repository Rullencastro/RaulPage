---
import Card from "./Card.astro";

interface Props {
	colSpan: number;
	rowSpan: number;
  order: number;
	orderMobile: number;
}

const {colSpan, rowSpan, order, orderMobile } = Astro.props;

---
<Card colSpan={colSpan} rowSpan={rowSpan} order={order} orderMobile={orderMobile}>
    <div class="w-full h-full flex justify-between px-20 items-center relative cursor-pointer overflow-hidden whitespace-nowrap">
        <div class="flex-shrink-0">
            <h2 class="text-black dark:text-white text-5xl font-bold pb-6">Raúl Castro Moreno</h2>
            <p class="text-gray-600 text-3xl">Ingeniero Software FullStack</p>
          </div>
          <div class="flex-shrink-0">
            <img src="/raulFotoRecortada.jpg" alt="Tu Foto" class="w-[400px] h-[400px] rounded-3xl object-cover overflow-hidden">
          </div>
    </div>
</Card>